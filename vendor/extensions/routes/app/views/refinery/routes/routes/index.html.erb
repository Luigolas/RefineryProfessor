<% content_for :body do %>
  <ul id="routes">
    <% @routes.each do |route| %>
      <li>
        <%= link_to route.name, refinery.routes_route_path(route) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
