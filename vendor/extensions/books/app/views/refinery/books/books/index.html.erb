<% content_for :body do %>
  <ul class="list-group" id="books">
    <% @books.each do |book| %>
      <li class="list-group-item">
        <%= link_to book.title, refinery.books_book_path(book) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
